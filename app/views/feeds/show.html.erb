<p id="notice"><%= notice %></p>
<p class = "image_file">
  <strong>Image:</strong>
  <p><%= image_tag @feed.image.url ,:size => '350x230' %><p>
</p>
<p>Content: <%= @feed.content %></p>
<p>Uploader: <%= @feed.user.name %></p>
<% unless @feed.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(feed_id: @feed.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %> |
<% if @feed.user.id ==current_user.id %>
  <%= link_to 'Edit', edit_feed_path(@feed) %>
<% end %>
 |<%= link_to 'Back', feeds_path %>
